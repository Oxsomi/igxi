name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: make build
      run: mkdir builds && cd ./build && cmake ../ -G "Unix Makefiles" && cmake --build . -j 8
