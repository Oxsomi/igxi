name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: cmake build
      run: cmake . -G "MinGW Makefiles"
    - name: build
      run: cmake --build . -j 8
