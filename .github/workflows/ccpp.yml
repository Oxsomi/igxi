name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: cmake build
      run: cmake . -G "Visual Studio 15 2017 Win64"
    - name: build
      run: cmake --build . -j 8
