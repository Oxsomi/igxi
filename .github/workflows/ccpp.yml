name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: cmake build
      run: mkdir builds & cd builds & cmake . -G "Unix Makefiles"
    - name: build
      run: cmake --build builds -j 8
